import 'package:flutter/material.dart';

class LabelStandard extends StatelessWidget {
  const LabelStandard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}